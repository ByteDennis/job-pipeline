[project]
name = "jobs-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jupyter>=1.1.1",
    "loguru>=0.7.3",
    "pandas>=2.3.3",
    "pip>=25.3",
    "sqlalchemy>=2.0.44",
]
