[input]
name = demo_run
step = step_meta
env = .env
category = dpst

[input.table]
file = input_table_list.xlsx
sheet = PCDS to AWS Mapping

[output]
folder = output

[output.next]
file = pipeline_state.json
